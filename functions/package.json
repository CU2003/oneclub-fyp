{
  "name": "oneclub-functions",
  "description": "cloud functions for oneclub (e.g. reporter approval email)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo \"no lint configured\"",
    "build": "echo \"no build step\"",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.6.0",
    "nodemailer": "^6.9.0"
  }
}

